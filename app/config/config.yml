imports:
    - { resource: parameters.yml }
    - { resource: security.yml }
    - { resource: services.yml }

framework:
    #esi:             ~
    translator:      { fallbacks: ["%locale%"] }
    secret:          "%secret%"
    router:
        resource: "%kernel.root_dir%/config/routing.yml"
        strict_requirements: ~
    form:            ~
    csrf_protection: ~
    validation:      { enable_annotations: true }
    templating:
        engines: ['twig']
        #assets_version: SomeVersionScheme
    default_locale:  "%locale%"
    trusted_hosts:   ~
    trusted_proxies: ~
    session:
        # handler_id set to null will use default session handler from php.ini
        handler_id:  ~
    fragments:       ~
    http_method_override: true

# Twig Configuration
twig:
    debug:            "%kernel.debug%"
    strict_variables: "%kernel.debug%"
    globals:
        fos_csrf_provider: "@form.csrf_provider"

# Assetic Configuration
assetic:
    debug:          "%kernel.debug%"
    use_controller: false
    bundles:        [mmitvBackBundle, FOSUserBundle]
    #java: /usr/bin/java
    filters:
        cssrewrite: ~
        #closure:
        #    jar: "%kernel.root_dir%/Resources/java/compiler.jar"
        #yui_css:
        #    jar: "%kernel.root_dir%/Resources/java/yuicompressor-2.4.7.jar"

# Doctrine Configuration
doctrine:
    dbal:
        driver:   "%database_driver%"
        host:     "%database_host%"
        port:     "%database_port%"
        dbname:   "%database_name%"
        user:     "%database_user%"
        password: "%database_password%"
        charset:  UTF8
        # if using pdo_sqlite as your database driver:
        #   1. add the path in parameters.yml
        #     e.g. database_path: "%kernel.root_dir%/data/data.db3"
        #   2. Uncomment database_path in parameters.yml.dist
        #   3. Uncomment next line:
        #     path:     "%database_path%"

    orm:
        auto_generate_proxy_classes: "%kernel.debug%"
        auto_mapping: true

# Swiftmailer Configuration
swiftmailer:
    transport: gmail
    username:  ngu.prisca@gmail.com
    password:  The159falL753

# FOS User Bundle on mmitvBackBundle, voir pour les options possibles // Template form
#https://github.com/FriendsOfSymfony/FOSUserBundle/blob/master/Resources/doc/configuration_reference.md
fos_user:
    db_driver: orm
    firewall_name: main
    user_class: mmitv\BackBundle\Entity\User
    use_listener : true
    use_flash_notifications : true
    use_username_form_type : true
    registration:
        confirmation:
            from_email:
                address: ngu.prisca@gmail.com
                sender_name: MMI TV
            enabled: true
            template: FOSUserBundle:Registration:email.txt.twig
    resetting:
        token_ttl : 86400
        email:
            from_email:
                address: ngu.prisca@gmail.com
                sender_name: MMI TV
            template: FOSUserBundle:Resetting:email.txt.twig
    service :
        mailer :                  fos_user.mailer.twig_swift
        email_canonicalizer :     fos_user.util.canonicalizer.default
        username_canonicalizer :  fos_user.util.canonicalizer.default
        token_generator :         fos_user.util.token_generator.default
        user_manager :            fos_user.user_manager.default